local decimal = 64;
-- Lua has a function called string.char(decimal) which converts decimal into character form. Lua searchs the ascii table for the decimal value and then returns the character version of the decimal value. 
print(string.char(decimal)); -- @
